{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities of the splash window in the DeepSeek Harness desktop shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
